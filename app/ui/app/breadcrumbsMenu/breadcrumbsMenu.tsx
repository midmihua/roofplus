import styles from "./breadcrumbsMenu.module.css";

const BreadcrumbsMenu = () => {
  return (
    <nav className={styles.container}>
      {"breadcrumbsMenu to > be > done > in > future"}
    </nav>
  );
};
export default BreadcrumbsMenu;
